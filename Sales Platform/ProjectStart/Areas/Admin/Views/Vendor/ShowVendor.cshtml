@model IEnumerable<DataLayer.Vendor>

@{
    ViewBag.Title = "Vendor";
}

<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.UserName)
        </th>
        <th></th>
        <th class="tr_Option_Vendor"></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserName)
            </td>
            <td>
                <a href="@Url.Action("SendToVendorSingle", "Vendor", new { id = item.UserIDRef, orderid = ViewBag.OrderID})" class="btn btn-success">ارسال</a>
            </td>
            <td class="tr_Option_Vendor">
                @*@Html.ActionLink("Edit", "Edit", new { id = item.GroupID })*@
                @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
            </td>
        </tr>
    }

</table>

